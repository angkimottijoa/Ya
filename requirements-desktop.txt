# Dependencies for desktop_app.py (the Windows .exe build). Tkinter ships
# with the standard python.org Windows installer, so it isn't listed here.
# Deliberately excludes gradio/app.py's deps to keep the frozen exe lean.
opencv-python-headless
numpy
Pillow
scikit-image
pynbt
pyinstaller
